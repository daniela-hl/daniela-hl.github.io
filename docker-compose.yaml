services:
  jekyll-site:
    image: jekyll-site
    build: .
    volumes: [ .:/usr/src/app ]
    ports: [ 4000:4000 ]
    environment: [ JEKYLL_ENV=docker ]
    command: bash -lc "bundle install && bundle exec jekyll serve -H 0.0.0.0 -w --config _config.yml"

